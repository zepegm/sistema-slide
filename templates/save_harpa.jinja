<!doctype html>
<html lang="pt-BR">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="Mark Otto, Jacob Thornton, and Bootstrap contributors">
    <meta name="generator" content="Hugo 0.88.1">
    <title>Edição de Hino da Harpa</title>

    <link rel="canonical" href="https://getbootstrap.com/docs/5.1/examples/album/">

    

    <!-- Bootstrap core CSS -->
    <link href="{{ url_for('static', filename='css/navbar-top-fixed.css') }}" rel="stylesheet">
    <link href="{{ url_for('static', filename='bootstrap/css/bootstrap.min.css') }}" rel="stylesheet">
    <link href="{{ url_for('static', filename='font-awesome/css/fontawesome.min.css') }}" rel="stylesheet">
    <link href="{{ url_for('static', filename='font-awesome/css/brands.css') }}" rel="stylesheet">
    <link href="{{ url_for('static', filename='font-awesome/css/solid.css') }}" rel="stylesheet">      
    <link href="{{ url_for('static', filename='css/style.css') }}" rel="stylesheet">
    

    <!-- Simular original -->
    <!-- <link href="https://fonts.googleapis.com/css?family=Poppins:300,400,500,600,700,800,900" rel="stylesheet">
		<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
		<link href="{{ url_for('static', filename='css/style.css') }}" rel="stylesheet"> -->

    <link rel="shortcut icon" type="image/png" href="{{ url_for('static', filename='images/Logo Colorido.png') }}">

    <style>
      @font-face {
          font-family:bebaskai;
          font-style:normal;
          font-weight:400;
          src:local('BebasKai'),
          url({{ url_for('static', filename='fonts/BebasKai-Regular.woff') }}) format('woff')
      }

      .bd-placeholder-img {
        font-size: 1.125rem;
        text-anchor: middle;
        -webkit-user-select: none;
        -moz-user-select: none;
        user-select: none;
        background-color: black;
      }

      .pagina {
          position: relative;
          top: 8px;
          left: 9px;
          width: 401px;
          height: 579px;
          border: solid;
          border-width: 1px;
          border-color: rgb(255, 0, 0, 0);
          color:black;
          margin-bottom: 16.3px;
      }

      .titulo {
          text-align: center;
          font-family:'BebasKai', sans-serif;
          color:rgb(0, 112, 192);
          font-size: 20px;
          position: relative;
          top:5px;
      }

      .content {
          font-size: 10px;
          font-family: Helvetica, Arial, sans-serif;
          position: relative;
          top: -1px;
          left: 26px;
          line-height: 1.3;
      } 

      .paragrafo {
        margin-bottom: 10px;
      }

      span.cdx-marker {
        padding-right: 3px;
      }

      .cdx-underline-view {
        text-decoration: underline;
        -webkit-text-underline-position: under;
        -ms-text-underline-position: below;
        text-underline-position: under; */
        /*text-decoration: none;
        border-bottom: solid;
        border-width: 0.1em;
        padding-bottom: 0px;*/
      }        

      .box-index {
        box-shadow: 1px 1px 2px black, 0 0 1em blue, 0 0 0.2em blue;
        color: blue;
        font-weight: bold;
      }

      @media (min-width: 768px) {
        .bd-placeholder-img-lg {
          font-size: 3.5rem;
        }
      }

    #anterior,
    #proximo {
      color: white;
    }

    #txt_titulo {
      border: solid;
      border-color: rgb(0, 0, 0, 0.3);
    }    

    #previewSlide {
      width: 426px; /* or whatever width you want. */
      height: 240px; /* or whatever width you want. */
      display: inline-block;    
      background-color: black;
      color: white;
      line-height: normal;
      font-family: Arial, Helvetica, sans-serif;
    }

    #previewSubtitle {
      width: 426px; /* or whatever width you want. */
      height: 50px; /* or whatever width you want. */
      display: inline-block;    
      background-color: rgb(0, 0, 0, 0.8); 
      color: white;
      line-height: normal;
      font-family: Arial;      
    }

    #preview_capa {
      background-image: url("{{ url_for('static', filename='images/Harpa.jpg') }}");  
      background-size: contain;
      background-repeat: no-repeat;
      background-size: 100% 100%;      

      width: 640px; /* or whatever width you want. */
      height: 360px; /* or whatever width you want. */      

      border: solid 1px;
    }

    #lb_numero {
      color:#978473;
      font-size:70px;
      font-style: italic;
      position: relative;
      left: 80px;
      top:-15px;
    }

    #lb_titulo {
      color:#978473;
      font-weight: bold;
      border:solid 1px rgb(0,0,0,0);
      position:relative;
      left: 20px;
      text-align: center;

      width: 320px;
      height: 80px;
    }

    #lb_autor {
      color:black;
      border:solid 1px rgb(0,0,0,0);
      position:relative;
      left: 20px;
      top:-20px;
      text-align: center;

      width: 320px;
      height: 60px;      
    }

    .arrows,
    .preview {
      width: 426px;
    }

    .view-marker {
      color:rgba(255,207,102);
      font-weight:bold;
      background:rgba(0,0,0,0)
    }

    .arrow {
      font-size: xx-large;
    }

    .doc {
      background-color: white;      
      border: solid;
      border-width: thin;
      border-color: rgb(0, 0, 0);
      color: black;
    }

    .box {
      padding:10px;
      padding-bottom: 20px;
      color:black;
      border-top: solid;
      border-bottom: solid;
      border-color: rgb(0, 0, 0, 0.3);
    }

    .blue {
      background-color:#b2d6ff;
      border-left: solid;
      border-right: solid;
      border-color: rgb(0, 0, 0, 0.3);
    }   

    .yellow {
      background-color:#fffed8;
    }   

    .pink {
      background-color:#ffe9ff;
      border-left: solid;
      border-right: solid;
      border-color: rgb(0, 0, 0, 0.3);      
    }   

    .txt-vinc {
      width: 100%;
      border: solid;
      border-color: rgb(0, 0, 0, 0.3);      
    } 

    .txt-vinc:hover:enabled {
      border-color: rgba(0, 162, 255, 0.7);  
    }

    .txt-vinc:focus {
      outline: none;
      border-color: rgba(0, 162, 255, 0.7);  
    }    

    textarea {
      resize: none;
    }

    #btn_gerar_pdf {
      height: 50px;
      width: 70px;
    }

    </style>
    
  </head>

  <body>

    {% block menu_superior %}
      {% include 'menu_superior.html' %}
    {% endblock %}


		<div class="wrapper d-flex align-items-stretch">
      {% block menu_lateral %}
        {% include 'menu_lateral.html' %}
      {% endblock %}

      <!-- Page Content  -->
      <div id="content" class="p-4 p-md-5 pt-5">
        <section class="container">
          <div id="preview_capa">
            <div id="lb_numero">HINO {{'%03d' % info['numero']|int}}</div>
            <div id="lb_titulo">{{info['titulo']}}</div>
            <div id="lb_autor">{{autor}}</div>
          </div>
          <div class="row">
            <div class="col-sm-12"><hr></div>
            <div class="col-sm-12">
              <button id="btn_back" class="btn btn-secondary"><i class="fa-solid fa-circle-left"></i> Retorar para Edição</button>
              <button id="btn_confirm" class="btn btn-primary"><i class="fa-solid fa-floppy-disk"></i> Confirmar Salvamento</button>
            </div>
            <div class="col-sm-12"><hr></div>
          </div>
          
          <div class="row">
            <div class="col-sm-6">
              <h3 class="fw-bold text-center">Lista dos Slides</h3>
              <table class="table table-striped">
                <thead>
                  <th>Nº</th>
                  <th>Texto</th>
                  <th>Categoria</th>
                </thead>
                <tbody>
                  {% for sld in info['slides'] %}
                    <tr class="text-center">
                      <th class="align-middle text-danger">{{sld['pos']}}</th>
                      <td class="align-middle">{{sld['text-slide']}}</td>
                      <td class="align-middle">
                        <select id="slide_cat_{{sld['pos']}}">
                          {% if cat_slides_list|length > 0 %}
                            {% set outer_loop = loop %}

                            {% for cat in cat_slides %}

                              {% if outer_loop.index0 < cat_slides_list|length %}

                                {% if cat_slides_list[outer_loop.index0]['categoria'] == cat['id'] %}
                                <option value="{{cat['id']}}" selected>{{cat['descricao']}}</option>
                                {% else %}
                                <option value="{{cat['id']}}">{{cat['descricao']}}</option>
                                {% endif %}

                              {% else %}
                                <option value="{{cat['id']}}">{{cat['descricao']}}</option>
                              {% endif %}

                            {% endfor %}                            
                          {% else %}
                            {% for cat in cat_slides %}
                              <option value="{{cat['id']}}">{{cat['descricao']}}</option>
                            {% endfor %}
                          {% endif %}
                        </select>
                      </td>
                    </tr>
                  {% endfor %}
                </tbody>
              </table>
            </div>

            <div class="col-sm-6">
              <div class="row">
                <div class="col-sm-12"><h3 class="fw-bold text-center">Prévia das Letras <button id="btn_gerar_pdf" class="btn btn-danger"><i class="fa-solid fa-file-pdf fa-xl"></i></button></h3></div>
                <div class="col-sm-12 doc"><div id="editorjs"></div></div>
                <h3>Página 2:</h3>
                <div class="col-sm-12 doc"><div id="editorjs2"></div></div>
              </div>
            </div>
          </div>
        </section>
      </div>
		</div>

  <form id="formBack" method="post" action="edit_harpa"><input id="json_back" name="json_back" type="hidden"></form>
  <form id="formSend" method="post" action="addHarpa"><input id="json_send" name="json_send" type="hidden"></form>

  <script src="{{ url_for('static', filename='js/jquery-3.6.0.min.js') }}"></script>
  <script src="{{ url_for('static', filename='js/popper.js') }}"></script>
  <!-- <script src="{{ url_for('static', filename='bootstrap/js/bootstrap.bundle.min.js') }}"></script> -->
  <script src="{{ url_for('static', filename='js/bootstrap.min.js') }}"></script>
  <script src="{{ url_for('static', filename='js/main.js') }}"></script>
  <script src="{{ url_for('static', filename='js/textFit.min.js') }}"></script>

  <script src="{{ url_for('static', filename='js/marker.umd.js') }}"></script>
  <script src="{{ url_for('static', filename='js/underline.umd.js') }}"></script>
  <script src="{{ url_for('static', filename='js/numeracao.js') }}"></script>
  <script src="{{ url_for('static', filename='js/editor-red.js') }}"></script>
  <script src="{{ url_for('static', filename='js/editorjs.umd.js') }}"></script>

  
<script>

textFit($("#lb_titulo"), {alignVert: true, multiLine: false, minFontSize: 20, maxFontSize: 230});
textFit($("#lb_autor"), {alignVert: true, multiLine: false, minFontSize: 20, maxFontSize: 230});


  $("#btn_gerar_pdf").on('click', function() {
    $(this).attr('disabled', '');
    $(this).html('<div class="spinner-border text-ligth" role="status"><span class="sr-only">Carregando...</span></div>');

    editor.save()
      .then( data => {

        var texto = [];

        for (item in data['blocks']) {
          aux = data['blocks'][item]['data']['text'].replaceAll("<mark ", "<span ").replaceAll("</mark>", "</span>").replaceAll('cdx-underline', 'cdx-underline-view');
          texto.push({'texto':aux})
        }

        editor2.save()
          .then( data_2 => {
            
            var texto_2 = [];

            for (item in data_2['blocks']) {
              aux = data_2['blocks'][item]['data']['text'].replaceAll("<mark ", "<span ").replaceAll("</mark>", "</span>").replaceAll('cdx-underline', 'cdx-underline-view');
              texto_2.push({'texto':aux})
            }

            info = [{'titulo':"{{'%03d' % info['numero']|int}}. {{info['titulo']}}", 'letras':texto, 'letras_2':texto_2, 'cont':'', 'pag':'1'}]
            console.log(info);

            $.ajax({
              type: "POST",
              url: "converto_to_pdf_list",
              contentType: "application/json",
              data: JSON.stringify(info),
              dataType: "json",
              success: function(data) { 
                $("#btn_gerar_pdf").removeAttr('disabled');
                $("#btn_gerar_pdf").html('<i class="fa-solid fa-file-pdf fa-xl"></i>');
                window.open(data);
              },
          });            
        }).catch((error) => {
          console.error('Saving error', error);
        });
      })
      .catch((error) => {
        console.error('Saving error', error);
      });    
  });

  function getTexto() {
    var texto = [];

        editor.save()
          .then( data => {

            cont = 1;

            for (item in data['blocks']) {
              texto.push({'paragrafo':cont, 'texto':data['blocks'][item]['data']['text'], 'pagina':1});
              cont++;
              //console.log(data['blocks'][item]['data']['text']);
            }

            editor2.save()
              .then( data_2 => {

                for (item in data_2['blocks']) {
                  texto.push({'paragrafo':cont, 'texto':data_2['blocks'][item]['data']['text'], 'pagina':2});
                  cont++;
                  //console.log(data['blocks'][item]['data']['text']);
                }                

                listaGeral = getInfo();
                listaGeral['letra'] = texto;
                listaGeral['destino'] = '{{destino}}';

                //console.log(listaGeral);

                $("#json_send").val(JSON.stringify(listaGeral));
                $("#formSend").submit();

              })
              .catch((error) => {
                console.error('Saving error', error);
              });
          })
          .catch((error) => {
            console.error('Saving error', error);
          });
          

  }

  function getInfo() {

    titulo = '{{info['titulo']}}';
    numero = '{{info['numero']}}';
    autor = '{{info['autor']}}'
    
    slides = {{info['slides']}};

    // acrescentando as categorias a lista
    for (item in slides) {
      slides[item]['cat'] = $("#slide_cat_" + slides[item]['pos']).val();
    }

    letras = [];

    return {'titulo':titulo, 'numero':numero, 'autor':autor, 'slides':slides};

  }

  $("#btn_back").on('click', function() {
    listaGeral = getInfo();
    lista_final = {'listaGeral':listaGeral, 'destino':'{{destino}}'};
    $("#json_back").val(JSON.stringify(lista_final));
    $("#formBack").submit();
  });

  $("#btn_confirm").on('click', function() {
    getTexto();
  });

  $("#add_vinc2").on('change', function() {
    if ($(this).val() != 0) {
      $("#status2").removeAttr('disabled');
      $("#desc_vinc2").removeAttr('disabled');
      $("#add_vinc3").removeAttr('disabled');
    } else {
      $("#status2").attr('disabled', '');
      $("#desc_vinc2").attr('disabled', '');
      $("#add_vinc3").val(0);
      $("#add_vinc3").attr('disabled', '');
      $("#status3").attr('disabled', '');
      $("#desc_vinc3").attr('disabled', '');      
    }
  });

  $("#add_vinc3").on('change', function() {
    if ($(this).val() != 0) {
      $("#status3").removeAttr('disabled');
      $("#desc_vinc3").removeAttr('disabled');
    } else {
      $("#status3").attr('disabled', '');
      $("#desc_vinc3").attr('disabled', '');
    }
  });  

  const editor = new EditorJS({
    /**
    * Id of Element that should contain Editor instance
    */
    holder: 'editorjs',

    inlineToolbar: ['bold', 'italic', 'underline', 'numeracao', 'red'],

    tools: {
      // ...
      underline: Underline,
      numeracao: {
        class: Numeracao,
        shortcut: 'CMD+SHIFT+M',
      },
      red: {
        class: Red
      }
    },
    data: {
      blocks: {{blocks}}
    }
  });

  const editor2 = new EditorJS({
    /**
    * Id of Element that should contain Editor instance
    */
    holder: 'editorjs2',

    inlineToolbar: ['bold', 'italic', 'underline', 'Marker'],

    tools: {
      // ...
      underline: Underline,
      Marker: {
        class: Marker,
        shortcut: 'CMD+SHIFT+M',
      }
    },
    data: {
      blocks: {{blocks_2}}
    }
  });  

</script>
      
  </body>
</html>
